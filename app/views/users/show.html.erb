<%= render 'headfoot/header' %>

<% if flash[:notice] %>
  <div class="flash">
    <%= flash[:notice] %>
  </div>
<% end %>

<div class="container">
  <div class="row">
    <div class="col-xs-4">
      <%= render 'memos/new', user: @user %>
    </div>

    <div class="col-xs-8">
      <h2>user投稿一覧</h2>
      <table class="table">
        <thead>
          <tr>
            <th></th>
            <th>Category</th>
            <th>Title</th>
            <th>Body</th>
          </tr>
        </thead>

        <tbody>
           <% @memos.each do |memo| %>
            <tr>
              <td></td>
              <td>
                 <%= memo.category %>
                </td>
              <td>
                <%= link_to memo_path(memo.id) do %>
                 <%= memo.title %>
                <% end %>
                </td>
              <td>
                 <%= memo.body %>
                </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
  <%= render 'headfoot/footer' %>
</div>